const config = {
    db: {
        host: 'localhost',
        user: 'nodejs',
        password: '@Nodejs12345',
        database: 'reactstickynotesdb',
        connectionTimeout: '-1'
    }
}

module.exports = config