const config = {
    db: {
        host: 'localhost',
        user: '<username>',
        password: '<password>',
        database: '<database>',
        connectionTimeout: '-1'
    }
}

module.exports = config